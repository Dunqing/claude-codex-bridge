{
  "name": "claude-codex-bridge",
  "version": "0.0.0",
  "description": "Bidirectional MCP server bridge between Claude Code and Codex CLI",
  "type": "module",
  "bin": {
    "claude-codex-bridge-codex": "./dist/codex-server.mjs",
    "claude-codex-bridge-claude": "./dist/claude-server.mjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "dev:codex-server": "tsx src/codex-server.ts",
    "dev:claude-server": "tsx src/claude-server.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src/ test/",
    "fmt": "oxfmt --write src/ test/",
    "fmt:check": "oxfmt --check src/ test/",
    "prepublishOnly": "pnpm build",
    "release": "bumpp",
    "prepare": "husky"
  },
  "keywords": [
    "mcp",
    "claude",
    "codex",
    "bridge",
    "ai"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "bumpp": "^10.4.1",
    "husky": "^9.1.7",
    "oxfmt": "^0.28.0",
    "oxlint": "^1.43.0",
    "tsdown": "^0.20.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
